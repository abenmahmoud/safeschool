import { getStore } from '@netlify/blobs';import type { Context, Config } from '@netlify/functions';import crypto from 'node:crypto';const SUPABASE_URL = Netlify.env.get('SUPABASE_URL') || 'https://bsytkpgdxvlddzuwaabp.supabase.co';const SUPABASE_KEY = Netlify.env.get('SUPABASE_SERVICE_ROLE_KEY') || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJzeXRrcGdkeHZsZGR6dXdhYWJwIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NTIwNjU1MiwiZXhwIjoyMDkwNzgyNTUyfQ.BdAhL5ammViYicizJjmmQxELcZore8LUQc5mR1giS7A';const RESEND_API_KEY = Netlify.env.get('RESEND_API_KEY') || '';const FROM_EMAIL = Netlify.env.get('NOTIFY_FROM_EMAIL') || 'notifications@safeschool.fr';const SUPERADMIN_EMAIL = Netlify.env.get('SUPERADMIN_EMAIL') || '';const SUPERADMIN_PASS = Netlify.env.get('SUPERADMIN_PASS') || '';const _cache = new Map();const TTL = 60000;const fromCache = (k) => { const c = _cache.get(k); return c && Date.now() - c.ts < TTL ? c.data : null; };const toCache = (k, d) => _cache.set(k, { data: d, ts: Date.now() });function genCode() {  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';  let c = 'SS-';  for (let i = 0; i < 6; i++) c += chars[Math.floor(Math.random() * chars.length)];  return c;}function cors(body, status = 200) {  return new Response(JSON.stringify(body), {    status,    headers: {      'Content-Type': 'application/json',      'Access-Control-Allow-Origin': '*',      'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',      'Access-Control-Allow-Headers': 'Content-Type, x-sa-token',    },  });}async function sbFetch(path, opts = {}) {
